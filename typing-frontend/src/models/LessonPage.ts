
export type LessonPage = {
  id?: number,
  duration: number,
  mistakes: number,
  backspaces: number,
  baseText: string,
  typedText: string,
  // mistakesMap: Map<string, number>,
  // correctsMap: Map<string, number>,
}
